# Система ролей и авторизации

## Обзор

В приложении реализована система ролей с двумя уровнями доступа:
- **USER** - обычный пользователь
- **ADMIN** - администратор

## Роли и права доступа

### ADMIN (Администратор)
**Полные права на:**
- ✅ Создание, редактирование, удаление продуктов
- ✅ Создание, редактирование, удаление категорий
- ✅ Просмотр всех пользователей
- ✅ Удаление пользователей
- ✅ Регистрация новых администраторов

### USER (Пользователь)
**Ограниченные права:**
- ✅ Просмотр продуктов (только чтение)
- ✅ Просмотр категорий (только чтение)
- ❌ Создание/редактирование/удаление продуктов
- ❌ Создание/редактирование/удаление категорий
- ❌ Просмотр списка пользователей
- ❌ Удаление пользователей

## API Endpoints с защитой

### Продукты (Products)

#### 🔒 Требует роль ADMIN:
```http
POST   /api/products          - Создание продукта
PATCH  /api/products/:id      - Обновление продукта
DELETE /api/products/:id      - Удаление продукта
DELETE /api/products/:id/soft - Мягкое удаление продукта
```

#### ✅ Доступно всем авторизованным пользователям:
```http
GET /api/products     - Получение всех продуктов
GET /api/products/:id - Получение продукта по ID
```

### Категории (Categories)

#### 🔒 Требует роль ADMIN:
```http
POST   /api/categories          - Создание категории
PATCH  /api/categories/:id      - Обновление категории
DELETE /api/categories/:id      - Удаление категории
DELETE /api/categories/:id/soft - Мягкое удаление категории
```

#### ✅ Доступно всем авторизованным пользователям:
```http
GET /api/categories     - Получение всех категорий
GET /api/categories/:id - Получение категории по ID
```

### Пользователи (Users)

#### 🔒 Требует роль ADMIN:
```http
GET    /api/user        - Получение всех пользователей
DELETE /api/user/:id    - Удаление пользователя
```

#### ✅ Доступно всем авторизованным пользователям:
```http
GET /api/user/:idOrEmail - Получение пользователя по ID или email
PUT /api/user            - Обновление своего профиля
```

### Аутентификация (Auth)

#### 🔒 Требует роль ADMIN:
```http
POST /api/auth/register-admin - Регистрация нового администратора
```

#### ✅ Доступно всем:
```http
POST /api/auth/register         - Регистрация обычного пользователя
POST /api/auth/register-first-admin - Регистрация первого администратора
POST /api/auth/login            - Вход в систему
GET  /api/auth/logout           - Выход из системы
GET  /api/auth/refresh-tokens   - Обновление токенов
```

## Создание первого администратора

Для создания первого администратора в системе используйте специальный эндпоинт:

```http
POST /api/auth/register-first-admin
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "admin123"
}
```

**Важно:** Этот эндпоинт можно использовать только один раз - для создания первого администратора. После создания первого админа этот эндпоинт будет возвращать ошибку.

## Создание дополнительных администраторов

После создания первого администратора, новые администраторы могут создаваться только существующими администраторами:

```http
POST /api/auth/register-admin
Authorization: Bearer <admin-token>
Content-Type: application/json

{
  "email": "newadmin@example.com",
  "password": "newadmin123"
}
```

## Примеры использования

### 1. Создание первого администратора
```bash
curl -X POST http://localhost:3002/api/auth/register-first-admin \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123"
  }'
```

### 2. Вход администратора
```bash
curl -X POST http://localhost:3002/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123"
  }'
```

### 3. Создание продукта (требует токен администратора)
```bash
curl -X POST http://localhost:3002/api/products \
  -H "Authorization: Bearer <admin-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "iPhone 15",
    "description": "Смартфон Apple iPhone 15",
    "categoryId": "uuid-категории",
    "price": 999.99,
    "stock": 50
  }'
```

### 4. Создание категории (требует токен администратора)
```bash
curl -X POST http://localhost:3002/api/categories \
  -H "Authorization: Bearer <admin-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Смартфоны",
    "description": "Мобильные телефоны и смартфоны"
  }'
```

### 5. Просмотр всех пользователей (требует токен администратора)
```bash
curl -X GET http://localhost:3002/api/user \
  -H "Authorization: Bearer <admin-token>"
```

## Обработка ошибок

### 403 Forbidden
Возвращается когда у пользователя недостаточно прав:
```json
{
  "statusCode": 403,
  "message": "Forbidden resource"
}
```

### 401 Unauthorized
Возвращается когда токен отсутствует или недействителен:
```json
{
  "statusCode": 401,
  "message": "Unauthorized"
}
```

## Техническая реализация

### Guards
- **JwtAuthGuard** - проверяет JWT токен
- **RolesGuard** - проверяет роли пользователя

### Decorators
- **@Roles(Role.ADMIN)** - указывает требуемую роль
- **@UseGuards(RolesGuard)** - применяет guard для проверки ролей
- **@Public()** - исключает эндпоинт из проверки авторизации

### Структура JWT токена
```json
{
  "id": "user-uuid",
  "email": "user@example.com",
  "roles": ["ADMIN"]
}
```

## Безопасность

1. **JWT токены** - для аутентификации
2. **Ролевая модель** - для авторизации
3. **Refresh токены** - для безопасного обновления сессий
4. **Валидация данных** - на всех входящих запросах
5. **Логирование** - всех операций с правами администратора

Система готова к использованию! 🚀 