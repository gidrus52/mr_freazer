# 🎯 ФИНАЛЬНОЕ РЕШЕНИЕ: Токены в Postman

## ✅ Проблема решена!

Ошибка `Cannot read properties of undefined (reading 'length')` была исправлена в JWT стратегии.

## 🔧 Правильная настройка Postman

### 1. **Порт сервера**
```
http://localhost:3000/api
```

### 2. **Получение токена**

#### Шаг 1: Создание пользователя
```
POST http://localhost:3000/api/auth/register
Headers:
  Content-Type: application/json
Body:
{
    "email": "testuser@example.com",
    "password": "testuser123"
}
```

#### Шаг 2: Вход в систему
```
POST http://localhost:3000/api/auth/login
Headers:
  Content-Type: application/json
Body:
{
    "email": "testuser@example.com",
    "password": "testuser123"
}
```

**Ответ:**
```json
{
    "accessToken": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 3. **Использование токена**

#### ✅ ПРАВИЛЬНО:
```
Headers:
  Authorization: <скопированный-токен-из-ответа>
```

#### ❌ НЕПРАВИЛЬНО:
```
Headers:
  Authorization: Bearer <токен>  // НЕ ДЕЛАЙТЕ ТАК!
```

## 📋 Примеры запросов

### Просмотр пользователей (требует ADMIN)
```
GET http://localhost:3000/api/user
Headers:
  Authorization: <admin-token>
```

### Создание категории (требует ADMIN)
```
POST http://localhost:3000/api/categories
Headers:
  Authorization: <admin-token>
  Content-Type: application/json
Body:
{
    "name": "Электроника",
    "description": "Электронные устройства"
}
```

### Публичный эндпоинт (не требует токен)
```
GET http://localhost:3000/api/categories
```

## 🛠️ Пошаговая инструкция для Postman

### Шаг 1: Создание коллекции
1. Откройте Postman
2. Создайте коллекцию "Auth API"

### Шаг 2: Настройка переменных
1. Перейдите в "Variables" коллекции
2. Добавьте переменную `baseUrl` = `http://localhost:3000/api`
3. Добавьте переменную `token` (оставьте пустой)

### Шаг 3: Создание запроса для входа
1. Создайте POST запрос
2. URL: `{{baseUrl}}/auth/login`
3. Headers: `Content-Type: application/json`
4. Body (raw JSON):
```json
{
    "email": "testuser@example.com",
    "password": "testuser123"
}
```

### Шаг 4: Автоматическое сохранение токена
1. В запросе входа перейдите в "Tests"
2. Добавьте скрипт:
```javascript
if (pm.response.code === 201) {
    const response = pm.response.json();
    pm.collectionVariables.set("token", response.accessToken);
}
```

### Шаг 5: Создание защищенного запроса
1. Создайте GET запрос
2. URL: `{{baseUrl}}/user`
3. Headers: `Authorization: {{token}}`

## 🔐 Тестовые данные

### Обычный пользователь:
- Email: `testuser@example.com`
- Password: `testuser123`
- Роли: `["USER"]`

### Администратор:
- Email: `admin@example.com`
- Password: `admin123`
- Роли: `["ADMIN"]`

## 🔍 Отладка

### Если получаете 401 Unauthorized:
1. Проверьте, что токен скопирован полностью
2. Не добавляйте дополнительный "Bearer " префикс
3. Получите новый токен через /api/auth/login

### Если получаете 403 Forbidden:
1. Используйте токен администратора для защищенных операций
2. Создайте администратора через /api/auth/register-first-admin

### Если сервер не отвечает:
1. Убедитесь, что сервер запущен на порту 3000
2. Проверьте логи сервера

## ✅ Чек-лист

- [ ] Сервер запущен на порту 3000
- [ ] Токен получен через /api/auth/login
- [ ] Токен скопирован полностью из ответа
- [ ] Не добавлен дополнительный "Bearer " префикс
- [ ] Используются правильные права доступа

## 🎉 Результат

Теперь токены должны работать корректно в Postman! Проблема с обработкой ролей была исправлена в JWT стратегии. 