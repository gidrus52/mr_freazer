# Руководство по работе с токенами в Postman

## 🔧 Настройка

### 1. Правильный порт
**Важно**: Сервер запускается на порту **3000**, а не 3002!

### 2. Получение токена

#### Шаг 1: Создание пользователя (если нужно)
```
POST http://localhost:3000/api/auth/register
Headers:
  Content-Type: application/json
Body:
{
    "email": "testuser@example.com",
    "password": "testuser123"
}
```

#### Шаг 2: Вход в систему
```
POST http://localhost:3000/api/auth/login
Headers:
  Content-Type: application/json
Body:
{
    "email": "testuser@example.com",
    "password": "testuser123"
}
```

**Ответ будет содержать:**
```json
{
    "accessToken": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 3. Использование токена

#### Важно: Токен уже содержит "Bearer " префикс!
Не добавляйте "Bearer " к токену, так как он уже включен в ответ сервера.

#### Правильный способ:
```
Headers:
  Authorization: <скопированный-токен-из-ответа>
```

#### Неправильный способ:
```
Headers:
  Authorization: Bearer <токен>  // НЕ ДЕЛАЙТЕ ТАК!
```

## 📋 Примеры запросов

### 1. Просмотр пользователей (требует ADMIN)
```
GET http://localhost:3000/api/user
Headers:
  Authorization: <admin-token>
```

### 2. Создание категории (требует ADMIN)
```
POST http://localhost:3000/api/categories
Headers:
  Authorization: <admin-token>
  Content-Type: application/json
Body:
{
    "name": "Электроника",
    "description": "Электронные устройства"
}
```

### 3. Создание продукта (требует ADMIN)
```
POST http://localhost:3000/api/products
Headers:
  Authorization: <admin-token>
  Content-Type: application/json
Body:
{
    "name": "iPhone 15",
    "description": "Смартфон Apple",
    "categoryId": "uuid-категории",
    "price": 999.99,
    "stock": 50
}
```

### 4. Публичные эндпоинты (не требуют токен)
```
GET http://localhost:3000/api/categories
```

## 🔍 Отладка проблем

### Проблема 1: "Невозможно соединиться с удаленным сервером"
**Решение**: Убедитесь, что используете порт 3000, а не 3002

### Проблема 2: 401 Unauthorized
**Возможные причины:**
- Токен истек (получите новый через /api/auth/login)
- Неправильный формат токена
- Проблемы с JWT_SECRET на сервере

### Проблема 3: 403 Forbidden
**Решение**: Используйте токен администратора для защищенных операций

### Проблема 4: 409 Conflict при регистрации
**Решение**: Пользователь уже существует, используйте другой email

## 🛠️ Пошаговая инструкция для Postman

### Шаг 1: Создание коллекции
1. Откройте Postman
2. Создайте новую коллекцию "Auth API"

### Шаг 2: Настройка переменных
1. Перейдите в "Variables" в коллекции
2. Добавьте переменную `baseUrl` = `http://localhost:3000/api`
3. Добавьте переменную `token` (оставите пустой)

### Шаг 3: Создание запроса для входа
1. Создайте POST запрос
2. URL: `{{baseUrl}}/auth/login`
3. Headers: `Content-Type: application/json`
4. Body (raw JSON):
```json
{
    "email": "testuser@example.com",
    "password": "testuser123"
}
```

### Шаг 4: Настройка автоматического сохранения токена
1. В запросе входа перейдите в "Tests"
2. Добавьте скрипт:
```javascript
if (pm.response.code === 201) {
    const response = pm.response.json();
    pm.collectionVariables.set("token", response.accessToken);
}
```

### Шаг 5: Создание защищенного запроса
1. Создайте GET запрос
2. URL: `{{baseUrl}}/user`
3. Headers: `Authorization: {{token}}`

## 🔐 Тестовые данные

### Обычный пользователь:
- Email: `testuser@example.com`
- Password: `testuser123`
- Роли: `["USER"]`

### Администратор (если создан):
- Email: `admin@example.com`
- Password: `admin123`
- Роли: `["ADMIN"]`

## 📝 Логирование на сервере

Сервер логирует:
- Все заголовки запроса
- Заголовок Authorization
- URL и метод запроса
- Процесс извлечения JWT токена

Проверьте логи сервера для отладки проблем с токенами.

## ✅ Чек-лист для проверки

- [ ] Используете порт 3000
- [ ] Токен скопирован полностью из ответа login
- [ ] Не добавляете дополнительный "Bearer " префикс
- [ ] Токен не истек (получите новый если нужно)
- [ ] Используете правильные права доступа (ADMIN для защищенных операций) 