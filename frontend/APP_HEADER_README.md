# AppHeader - Собственный хедер для страницы App

## Описание

Создан уникальный хедер для страницы App с собственным дизайном и функциональностью, отличный от основного хедера сайта.

## Особенности

### 1. **Уникальный дизайн**
- Собственный стиль, не связанный с основным хедером
- Современный и привлекательный интерфейс
- Адаптивная сетка с использованием Naive UI

### 2. **Функциональность**
- **Логотип и название** - 📱 App с фирменным стилем
- **Поиск** - поле поиска с кнопкой для поиска в приложении
- **Навигация** - кнопки для перехода на главную и в кабинет/вход
- **Поднавигация** - дополнительные пункты меню для App

### 3. **Адаптивность к авторизации**
- Если пользователь **авторизован** - показывается кнопка "👤 Кабинет"
- Если пользователь **НЕ авторизован** - показывается кнопка "🔑 Войти"
- Динамическое изменение интерфейса в зависимости от статуса

### 4. **Логика "To Main"**
- При нахождении на странице App название "Обзор" меняется на "← To Main"
- При нажатии на "← To Main" происходит переход на главную страницу
- Аналогично логике основного хедера

## Структура компонента

### **AppHeader.tsx**
```typescript
// Верхняя часть хедера
- Логотип "📱 App"
- Поле поиска с кнопкой
- Навигационные кнопки (Главная, Кабинет/Войти)

// Нижняя навигация
- Обзор (меняется на "← To Main" при активной странице)
- Возможности
- Скачать
- Поддержка
```

### **AppView.tsx**
```typescript
// Основная страница App
- AppHeader компонент
- Заголовок страницы
- Карточки функций (Быстрота, Безопасность, Простота)
- Демонстрация приложения
- Статистика
- Призыв к действию
```

## Технические детали

### **Импорты**
```typescript
import {defineComponent, computed} from "vue";
import {RouterLink} from "vue-router";
import {router} from "../../../router";
import {isAuthenticated} from "../../../utils/api";
import {NGrid, NGridItem, NButton, NInput, NInputGroup} from 'naive-ui'
```

### **Состояние**
```typescript
const currentRoute = computed(() => router.currentRoute.value)
const isUserAuthenticated = computed(() => isAuthenticated())
```

### **Стилизация**
- Использование inline стилей для гибкости
- Цветовая схема: #007bff (основной), #28a745 (успех), #17a2b8 (информация)
- Адаптивная сетка с максимальной шириной 1200px
- Отступы и границы для визуального разделения

## Тестирование

Создан тестовый файл `test-app-header.html` для проверки функциональности:

### **Возможности тестирования:**
1. **Вход в систему** - имитирует авторизацию пользователя
2. **Выход из системы** - очищает данные авторизации
3. **Проверка статуса** - показывает текущий статус авторизации
4. **Очистка localStorage** - полностью очищает все данные
5. **Смена страницы** - демонстрирует изменение "To Main"

### **Что тестируется:**
- Отображение хедера для неавторизованных пользователей
- Отображение хедера для авторизованных пользователей
- Изменение навигации при смене статуса авторизации
- Логика "To Main" для страницы App
- Поиск и навигационные элементы

## Отличия от основного хедера

| Аспект | Основной хедер | AppHeader |
|--------|----------------|-----------|
| **Дизайн** | Минималистичный, корпоративный | Современный, привлекательный |
| **Поиск** | Только на странице Advertisement | Всегда доступен |
| **Навигация** | Горизонтальное меню | Комбинированная (верх + низ) |
| **Логотип** | Иконка замка | 📱 App с эмодзи |
| **Стиль** | Naive UI компоненты | Кастомные стили + Naive UI |

## Зависимости

- Vue 3 с Composition API
- Vue Router 4
- Naive UI для компонентов
- TypeScript для типизации
- @vicons/ionicons4 для иконок

## Запуск

1. Установите зависимости: `npm install`
2. Запустите dev сервер: `npm run dev`
3. Перейдите на страницу `/app`
4. Откройте тестовый файл `test-app-header.html` для проверки функциональности

## Примечания

- AppHeader полностью независим от основного хедера
- Использует ту же логику авторизации через `isAuthenticated()`
- Собственная стилизация и дизайн
- Адаптивная сетка для разных размеров экрана
- Поддержка логики "To Main" как в основном хедере
