# 🔐 Решение проблемы аутентификации при создании категорий

## Проблема
Ошибка: "Ошибка авторизации. Пожалуйста, войдите в систему заново." при попытке создания категории.

## Диагностика

### 1. Проверьте состояние аутентификации
Откройте консоль браузера и выполните:

```javascript
// Проверка localStorage
console.log('authToken:', localStorage.getItem('authToken'));
console.log('isAdmin:', localStorage.getItem('isAdmin'));
console.log('adminUser:', localStorage.getItem('adminUser'));

// Проверка валидности токена
function isValidToken(token) {
    if (!token) return false;
    if (token.length < 10) return false;
    if (token.includes('test_token') || token.includes('fake')) return false;
    return true;
}

const token = localStorage.getItem('authToken');
console.log('Токен валидный:', isValidToken(token));
```

### 2. Используйте тестовую страницу
Откройте `test-auth.html` в браузере для интерактивной диагностики.

## Возможные причины и решения

### Причина 1: Токен отсутствует или невалидный
**Симптомы:**
- `authToken` отсутствует в localStorage
- Токен слишком короткий или содержит тестовые значения

**Решение:**
1. Войдите в систему заново
2. Проверьте, что сервер возвращает валидный токен

### Причина 2: Флаг isAdmin не установлен
**Симптомы:**
- `isAdmin` отсутствует в localStorage
- Токен есть, но аутентификация не проходит

**Решение:**
1. Очистите localStorage: `localStorage.clear()`
2. Войдите в систему заново

### Причина 3: Токен истек
**Симптомы:**
- Токен есть, но сервер возвращает 401
- Запросы к API категорий не проходят

**Решение:**
1. Выйдите из системы
2. Войдите заново для получения нового токена

### Причина 4: Проблемы с сервером
**Симптомы:**
- Все данные аутентификации корректны
- Сервер возвращает ошибки 500 или другие

**Решение:**
1. Проверьте, что сервер запущен
2. Проверьте логи сервера
3. Убедитесь, что API endpoints работают

## Пошаговое решение

### Шаг 1: Очистка данных
```javascript
localStorage.clear();
```

### Шаг 2: Вход в систему
1. Откройте страницу входа
2. Введите корректные данные администратора
3. Проверьте, что токен сохранился

### Шаг 3: Проверка аутентификации
```javascript
// Проверьте, что все данные сохранены
console.log('Проверка после входа:');
console.log('- authToken:', localStorage.getItem('authToken') ? 'сохранен' : 'не найден');
console.log('- isAdmin:', localStorage.getItem('isAdmin'));
console.log('- adminUser:', localStorage.getItem('adminUser') ? 'сохранен' : 'не найден');
```

### Шаг 4: Тест создания категории
1. Откройте страницу управления категориями
2. Попробуйте создать новую категорию
3. Проверьте консоль на наличие ошибок

## Отладочные инструменты

### 1. Тестовая страница
Файл `test-auth.html` содержит интерактивные инструменты для диагностики.

### 2. Улучшенный API
Файл `src/utils/api-improved.ts` содержит более детальную диагностику.

### 3. Отладочные скрипты
- `debug-auth-status.js` - проверка состояния аутентификации
- `debug-auth.js` - тестирование входа
- `debug-headers.js` - проверка заголовков запросов

## Проверка работоспособности

После исправления проблемы:

1. ✅ Вход в систему работает
2. ✅ Токен сохраняется в localStorage
3. ✅ Флаг isAdmin установлен
4. ✅ Запросы к API категорий проходят
5. ✅ Создание категорий работает

## Контакты для поддержки

Если проблема не решается:
1. Проверьте логи сервера
2. Убедитесь, что API endpoints настроены правильно
3. Проверьте CORS настройки
4. Убедитесь, что база данных доступна 