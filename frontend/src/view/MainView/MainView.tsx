import MainPage from '../../components/main'
import {defineComponent} from "vue";

export default defineComponent(
    {
        setup () {
            return {

            }
        },
        render () {
            return (
                <MainPage></MainPage>
            )
        }
    }
)