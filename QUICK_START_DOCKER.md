# 🚀 Быстрый запуск в Docker

## На сервере выполните:

### 1. Перейдите в директорию проекта

```bash
cd /opt/mr_freazer
```

Если проект еще не клонирован:

```bash
cd /opt
git clone https://github.com/gidrus52/mr_freazer.git
cd mr_freazer
```

### 2. Создайте файл .env (рекомендуется)

```bash
nano .env
```

Добавьте (или используйте значения по умолчанию):

```env
POSTGRES_PASSWORD=ваш_пароль
JWT_SECRET=ваш_секретный_ключ
REFRESH_SECRET=ваш_секретный_ключ
```

### 3. Запустите все в Docker

```bash
docker-compose up -d
```

### 4. Проверьте статус

```bash
docker-compose ps
docker-compose logs -f
```

### 5. Готово! 

Приложение доступно по адресу: `http://ваш_сервер_ip`

---

## Полезные команды:

```bash
# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Обновление
git pull
docker-compose up -d --build
```

Подробная документация: `DOCKER_START.md`

